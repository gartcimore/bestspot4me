# Kill all running containers.
alias dockerkillall='docker kill $(docker ps -q)'

# Delete all stopped containers.
alias dockercleanc='printf "\n>>> Deleting stopped containers\n\n" && docker rm $(docker ps -a -q)'

# Delete all untagged images.
alias dockercleani='printf "\n>>> Deleting untagged images\n\n" && docker rmi $(docker images -q -f dangling=true)'

# Delete all stopped containers and untagged images.
alias dockerclean='dockercleanc || true && dockercleani'

#start mongodb
alias start_db='docker run --name  some-mongo -p 27017:27017 -d mongo'

#configure env
alias configure=''
alias bestSpot4me_home='cd /vagrant/webapp'
alias bestSpot4me_build='cd /vagrant/webapp && npm install && npm run build'
alias bestSpot4me_start='cd /vagrant/webapp && npm start'
